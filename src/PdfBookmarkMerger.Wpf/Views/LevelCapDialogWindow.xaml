<ui:FluentWindow x:Class="PdfBookmarkMerger.WpfApp.Views.LevelCapDialogWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="子要素のレベル上限を設定"
                 Icon="pack://application:,,,/Assets/app.ico"
                 Width="400" SizeToContent="Height"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="None"
                 PreviewKeyDown="OnWindowPreviewKeyDown">
    <Grid Margin="16,40,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="子要素のレベル上限を設定" Margin="-16,-40,-16,8" VerticalAlignment="Top">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/app.ico" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,8">
                選択した項目の下位要素のうち、ここで指定したレベルより深い要素はすべて削除されます。
            </TextBlock>
            <TextBlock Text="レベル上限" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding AvailableLevels}"
                      SelectedItem="{Binding SelectedLevel.Value, Mode=TwoWay}"
                      Background="{DynamicResource ComboBoxDropDownBackground}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource SolidBackgroundFillColorTertiaryBrush}" Padding="4,3">
                            <TextBlock Text="{Binding StringFormat=Lv.{0}}" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        </Border>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button Content="キャンセル" Click="OnCancelClick" Margin="0,0,8,0" />
            <ui:Button Content="OK" Appearance="Primary" Click="OnOkClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
